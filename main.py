# This is a simple Python script that defines a function to sum two numbers
# and prints the result of calling that function with two arguments.
def sum(value1, value2):
    """
    This function takes two values and returns their sum.
    """
    return value1 + value2

print(sum(1, 2))